{
  "extends": "./tsconfig.json",
  "compilerOptions": {
    "importHelpers": false,
    "target": "es2018",
    // workaround for a bug in ts-jest where moduleResolution gets overwritten
    // TODO: try removing this when ts-jest is updated
    "moduleResolution": "Classic"
  }
}
