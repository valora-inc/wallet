import Tokens from '@ubeswap/default-token-list/ubeswap.token-list.json'
import { map, reduce } from 'lodash'
import { StoredTokenBalance, StoredTokenBalances } from 'src/tokens/reducer'
const { tokens } = Tokens

export default AlfajoresTokens
