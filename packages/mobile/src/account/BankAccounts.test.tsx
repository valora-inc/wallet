import { render } from '@testing-library/react-native'
import * as React from 'react'

describe('BankAccounts', () => {
  it('renders bank acount info', () => {
    expect(true)
  })
  it('re-fetches bank account info when the newPublicToken navigation prop changes', () => {
    expect(true)
  })
  it('can add new bank accounts', () => {
    expect(true)
  })
  it('can can delete bank accounts', () => {
    expect(true)
  })
})
